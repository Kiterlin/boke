# 站点公开访问地址：用于生成 RSS / sitemap / Open Graph 的绝对链接。
# 本地开发可留空（默认 http://localhost:3000）。
# 部署到 Vercel 后，在 Settings → Environment Variables 设为分配的地址，
# 例如 https://deepframe-lab.vercel.app
NEXT_PUBLIC_SITE_URL=
